# CWSI 플랫폼 연동 및 고도화 로드맵

작성: 2026-09-02 | 전제: 현 단계는 **기상 데이터 없이 v2 파이프라인 그대로 플랫폼 연동**, 기상대 연동·캘리브레이션은 중장기 추진

---

## 단계 요약

| 단계 | 시기(안) | 핵심 | 기상데이터 | 제공 가능 서비스 수준 |
|---|---|---|---|---|
| **Phase 1 — 플랫폼 연동 (현재)** | ’26.9 ~ | v2 산출물(GeoTIFF+CSV) 그대로 연동, QC 게이트 정책 탑재 | 불필요 | 필지 내 상대 진단·구역 탐지, 스카우팅 우선순위 |
| **Phase 2 — 기상 연동** | ’27 | 새만금 간이기상대 데이터 파이프라인 연결, 촬영시각 매칭 | 시간별 기온·습도·일사 | ΔT 정밀화, 청천 자동 QC — 서비스 수준은 Phase 1과 동일하나 신뢰도 향상 |
| **Phase 3 — 절대화 (CWSI 로직 고도화)** | ’27 시즌 | NWSB 기준선 도입 (ΔT-VPD 회귀) | VPD(기온·습도) + 캠페인 비행 | **필지 간·날짜 간 절대 비교** |
| **Phase 4 — 로직 확장·통합 연계 (장기)** | ’28 ~ | 일단위 CWSI 검토, 타 분석(결주 등) 결합 | 풍속·일사·ET 활용 | 일단위 지수·통합 진단 |

---

## Phase 1 — 기상 없이 플랫폼 연동 (현재, 즉시 착수 가능)

**목표**: v2 파이프라인 산출물을 관제 플랫폼에 연동하여 "필지 내 상대 진단" 서비스 개시

**연동 작업 (플랫폼 측)**
1. **GeoTIFF 연동**: `result_cwsi_v2/{태그}_CWSI.tif` (EPSG:5179, 0~1, nodata −9999) → 지도 오버레이/구역 통계
2. **CSV 연동**: `cwsi_summary.csv` → 필지 카드·대시보드 (평균/중앙값/p90 CWSI, 스트레스 화소비율 `frac_gt_05`, ΔT, 식생비율)
3. **QC 게이트 구현 (필수)**: `qc` 열이 비어있지 않으면 해당 필지·회차를 **"판정 보류"로 표시** — 값 노출은 하되 등급 판정·알림에서 제외하는 정책 권장
4. **운영 규칙 코드화**: 노화기(9월 하순 이후) 회차는 수분스트레스 판정 비활성, 회차 메타에 "판정용/모니터링용" 구분 태그

**파이프라인 측 잔여 작업**
- 손상 파일 2건(SM16_02 LWIR, SM18_02 RGB) 재확보 → `python cwsi_index.py SM16 SM18` 재처리
- 촬영 시각 기록 체계 시작 (비행로그/EXIF/촬영대장) — Phase 2 준비물

**이 단계의 서비스 한계 (고객 안내 문구)**: CWSI 값은 필지 내 상대값으로, 구역 간 비교·시계열 패턴 추적에 유효. 필지 간 절대 비교와 "관수하라/말라" 절대 판정은 Phase 3 이후 제공.

---

## Phase 2 — 기상대 데이터 연동 (’27)

**목표**: 이미 설치된 새만금 간이기상대(시간별 기온·습도·강수·풍속·일사·증발산)를 파이프라인에 연결

**작업**
1. **기상 어댑터 구조**: `weather_data.csv` 표준 포맷 (datetime, temp, RH, rain, wind, solar, ET) 뒤에 소스별 어댑터 — ① 새만금 간이기상대(1순위) ② **기상청 API(공공데이터포털 ASOS·AWS 시간자료)** — 결측 백업·교차 검증 및 **기상대 없는 신규 지역 확장 대응**
2. **촬영시각 매칭 ΔT**: 현재 일 대표기온 1개 → 촬영 시각의 실제 기온으로 교체 (ΔT 지표 정밀화)
3. **일사량 기반 청천 QC**: 촬영 시각 일사량이 임계 미만이면 `qc`에 `low_solar` 기록 — "맑은 날만 유효" 규칙의 자동화
4. VPD 계산 모듈 탑재 (Phase 3 준비): es=0.6108·exp(17.27T/(T+237.3)), VPD=es·(1−RH/100)
5. 소스 검증 1회: 간이기상대 vs 기상청 최근접 지점(군산 ASOS 등) 한 달치 기온·습도 비교 → 편차 일정 시 보정계수 적용. NWSB 회귀식은 생성/사용 소스 일치 원칙

**산출물**: 파이프라인 v2.1 (기상 로더 + 시각 매칭 + 청천 QC)

---

## Phase 3 — 절대화: CWSI 로직 고도화 (’27 재배 시즌)

**목표**: 장면-상대 CWSI의 구조적 한계 2가지(필지 간 절대 비교 불가, 균일 장면 판정 보류)를 **로직 고도화(NWSB)** 로 해소

**작업 — NWSB(무수분스트레스 기준선) 도출 및 탑재**
- 시기: 강우/관수 직후 맑은 날 (기상대 강수량으로 자동 선정), 개화기 1일 + 등숙기 1일
- 방법: 10~16시 4~6회 드론 비행 → ExG 마스크 캐노피 평균온도 + 해당 시각 VPD 짝짓기 → ΔT = m·VPD + b 회귀 (목표 R²≥0.8, VPD 범위 2kPa+)
- 코드: `BASELINE_MODE="nwsb"` 분기 추가 (Twet/Tdry를 기상 기반으로 산출), 백분위 방식과 병행 운영(A/B)으로 전환 타당성 검증

**작업 — v2 타당성 실증 (검증 액션 플랜)**
- **농가 VoC 교차검증**: 스트레스 상위 판정 필지(예: SM21·SM23)의 현장 확인·농가 인터뷰 결과와 CWSI 등급 대조 — "지수가 높던 필지에서 실제 문제가 있었는가"를 시즌 단위로 축적
- **독립 지표 교차검증**: ΔT(작물온도−기온) 순위와 CWSI 순위의 일치도 확인 (기상 연동 후 정밀화)
- **NWSB 병행 비교(A/B)**: 기상 기반 기준온도와 백분위 방식의 필지 순위·등급 일치도 분석

**달성 시 서비스 확장**: 필지 간 랭킹·지역 단위 스트레스 지도, 9월 균일 장면 판정 가능화

> (참고) 절대 임계 기반 등급 판정·관수 처방은 토양수분 실측 검증이 선행돼야 하므로 본 로드맵 범위에서 제외 — 추후 별도 과제로 검토

---

## Phase 4 — 로직 확장·통합 연계 (장기, ’28~)

- **일단위 CWSI 검토**: 기상대의 풍속·일사·ET로 Penman-Monteith 기반 CWSIt (Combining UAV 방식) — 순간값의 일 대표성 한계 극복
- **분석 결합**: 결주 분석(위성·드론)과 공간 중첩 → 저활력 원인 진단(수분 vs 결주 vs 병해)
- **다지역 확장**: NWSB 계수의 지역·품종 이식성 검증
- (추후 검토) 관수 권고·VRA 연계 — 실측 검증 과제와 연동 시

---

## 의사결정 포인트 (경영·개발 협의용)

1. **Phase 1 게이트 정책**: QC 경고 필지를 "숨김"으로 할지 "보류 표시"로 할지 — 보류 표시 권장 (데이터 투명성)
2. **Phase 3 캠페인 일정**: NWSB 도출용 드론 비행 2일 (개화기·등숙기 각 1일, 강우/관수 직후 맑은 날)
3. **촬영 표준 준수**: 전 Phase 공통 — 맑은 날 12~16시, 개화~등숙기 중심 (미준수 시 어떤 고도화로도 보정 불가)
